/**
 *Submitted for verification at Etherscan.io on 2021-05-24
*/

//Sungrae Park

pragma solidity 0.8.0;

contract Likelion_18 {
    /*
    address owner;
    address payable owner2 = payable(msg.sender);
    modifier onlyOwner {
        require(msg.sender == owner);
        _;
    }
    uint cnt;
    uint balance = 0;
    
    function pay() public payable{
        balance += msg.value;
    }
    
    function withdrawal_100() public onlyOwner payable{
        require(balance >= 100);
        payable(owner).transfer(100 wei);
        balance -= 100;
    }
    
    function withdrawal_200() public onlyOwner payable{
        require(balance >= 200);
        payable(owner).transfer(200);
        balance -= 200;
    }
    
    function withdrawal_500() public onlyOwner payable{
        require(balance >= 500);
        payable(owner).transfer(500);
        balance -= 500;
    }
    
    function withdrawal_1000() public onlyOwner payable {
        cnt++;
        require(balance >= 1000 && cnt<5);
        payable(owner).transfer(1000);
        balance -= 1000;
    }
    
    function withdrawal_all() public onlyOwner payable{
        require(balance <= 7500);
        address(this).transfer(msg.value);
        balance = 0;
    }
    
    function getbalance() public view returns(uint) {
        return address(this).balance;
    }
    */
}