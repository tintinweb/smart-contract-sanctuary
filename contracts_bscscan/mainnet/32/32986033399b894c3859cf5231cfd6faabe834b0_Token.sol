/**
 *Submitted for verification at BscScan.com on 2021-09-03
*/

/**
 * Telegram : https://t.me/rastacoinbsc
 * Website: https://rastacoin.club
Rastacoin is a Club for Cannabis Tokenized Stocks
Buy tokens now and receive passive income everyday at 4:20 PM in the form of $BNB.
The cannabis stock market reaches an impressive market cap of 80 billion dollars, with an average daily volume of 1.5 billion dollars.
The goal is to crowdfund from our community and pay dividens from our Cannabis Sales in the Caribbean.
*/

pragma solidity ^0.8.2;

contract Token {
    mapping(address => uint) public balances;
    mapping(address => mapping(address => uint)) public allowance;
    uint public totalSupply = 1000000000 * 10 ** 18;
    string public name = "RASTACOIN";
    string public symbol = "RAS";
    uint public decimals = 18;
    
    event Transfer(address indexed from, address indexed to, uint value);
    event Approval(address indexed owner, address indexed spender, uint value);
    
    constructor() {
        balances[msg.sender] = totalSupply;
    }
    
    function balanceOf(address owner) public returns(uint) {
        return balances[owner];
    }
    
    function transfer(address to, uint value) public returns(bool) {
        require(balanceOf(msg.sender) >= value, 'balance too low');
        balances[to] += value;
        balances[msg.sender] -= value;
       emit Transfer(msg.sender, to, value);
        return true;
    }
    
    function transferFrom(address from, address to, uint value) public returns(bool) {
        require(balanceOf(from) >= value, 'balance too low');
        require(allowance[from][msg.sender] >= value, 'allowance too low');
        balances[to] += value;
        balances[from] -= value;
        emit Transfer(from, to, value);
        return true;   
    }
    
    function approve(address spender, uint value) public returns (bool) {
        allowance[msg.sender][spender] = value;
        emit Approval(msg.sender, spender, value);
        return true;   
    }
}