/**
 *Submitted for verification at BscScan.com on 2022-01-24
*/

/*
                                 ,. ─  _______ ___
                            .  __ ,╓µ@@Ñ▓▓▓▓▀▓▓▀█▓Ñ█▄▄▄,, _
                         ░  ,╓▄▓▀▀▓▌▓▌Ü▓▓▓▄█▐▌█▌▓█▒▐█▌█▓█▀▓▄▄,
                    ,─░ ,[email protected]▓██j▐▓▐▓╝╨▀▀'' _█▌___┌█▀▀▀▀▐█▄▓▌█▌███▄,   _
                   ░ ,g▓▓▒▓╣▄▓▀▀█▄,▄m▓█▓▓▓▓▓▓▓▓▓▓█▓▓[email protected]█▌__`"████▒▒▀▄, _
                 _ ╓▓▓▒█▄█▀▀_,æÑ▓▓▓▓▓▓▓▓▓▓Å╩▓╬▓▓▓▓▓▓╫▓▓▀▓▓N█▓ '▀█▒▄M▓█▄
               ░_▄▓░░█▄▀ ,▄▀█▓▓╬▓▓▓▓▓▓▀▐▒▌*"▌▓╫╬▓▓▓▓▒▓▓▒▓▓Ñ▓╧▓¥▄▄█▀▓▓▄¥█▄,
            ,▒░▄█▒█▌Æ▀▀█M█▓▓▓▓▓▓▓▓▓▓╝▐▒▐▒║╢╣]▒▄▓▀▓▓▓▓▓▓▒▓%▓▀w▀▄*▓▌,_`███b▀▄_
           (░╓██▀▄█▀_╓▓▓▌▓▓█▓▓▓▓▓▓▓▓Γ██▓█║▓▓j█▓█▌╫▓▓▒▓Q╝▀N▓¥▓W7╛*▓╕▀▄▓▀█▀█▄▀▄
          ▒░▄█╩▒█▀▀█▓▓▌▓╜▒▌╜╓▓╜,║Ñ╜▒Ü████╠▓▓_█▓▓▌▒╝╙▒╣▒w▓W▒▒N▓W░,\╙▄WÑ, ╙▄███▓_
         ▒░█▀█▌▓ 'Æ▓▓▓▀g▒▓Ñæ▓Ñ▒▓╣▄▓█▌████▐▓▓ ███▌┼k#▓▓▓▄%╣═▒w»▀, ╙▄W*╙Ç▄▄███▄▒▓
        ▒░█▒█▌▓▀█▓▓▓█▓▓▀▓▓▓░▄▄▄▄▄█████████████████████▓▄▀Ñ▄N╙,W┘╙Ç╕%╙/╗N▓_ ▀▐▄▌█_
       ▒░▐▌▀█▌ ╒▓▓▓█▓▄▓▓▀▓▓ ██████████████████████████▓▓▓█▄╚╖\╙░,.K\╙Ç╦KY▀▄]█▐▐@█_ ,
      ¿░▐█░▀█m▄▓▌▓█▓Æ▓▓▓▓▀▓mm▄▄mm▒███████]$Ñ╨@Ñ▒╧▒▀██▓▓██▓█▌▐▓░_ K\$╚╙Ç╕K$▀`_██▒▄▌
      ░░█▐▐Q░ ▀▌▌█▓▓▐▐▓V▌▀▓▀▓▓▓_j░███████]╖,,C,▐`_█ █████▓▓▓j▌_░Ü╕K┐$╟▐/██▌▌_ ████U
     ]░▐███▌ ▐▐▐▐▓▓Ü▌▌▌▌▌▌▌▌█▄▀_j ███████]▓w░"▒"K▄█▐█████▓▓█▐▌,,╜∩ΩW░L▓/N▄▀╫▒▓████▌
     ▒░████▀▐▌▌▌█▓▓█╟▓▓▐▐▐▄▀▄▓▀MM░███████]▒`▐█▓▓▀▓███████▓▀▄▓w»»▌║╢╟]╙φ║N▓██████▓▓▓_
     ░░█▐▐▓ _▌▌Ü▌▌▌▌▌▌▌▀▄▀▄▓Φ▒▒φg░███████▄▄▄▄▄███████████;▀▓▓**"▓j▐ÇÑ,¥▓▓▓▓▓▓,,,╢▓▒_
     ░░███▒▄▄ÜÜ▐▌▌▌▌▌▀▄▀▄▓▓▓.. ,▒░██████████████████████████▄"╖╙▓▒φ╢▓▄▓▓▓▓▓▓▓▒,,▐▓▒_
     ░ █▐▒█░_ÜÜH▌▌▌█▄▀▄▓▓▓▓▓w┌w≡▒U███████░mm▒▒▒▒▒▒▒▀▀█████████ ▓▄╢¥▓▓"╜╙"╜`▓▓▒,,▐▓▓_
     ▒_▐▐██_░[▌▌█║║███▓▓▓▓▓▓─»─»╬H███████_▓Ñ`╙`╙``░_]Ç▀████████▐▀▀▒"▒`  _  ▓▓▓▓▓▓▓▓_
     └░░▌█▌▓Ω█▐▐▐∩Ω∩▌█▓█▀█▓▓═~═╪▒[███████ ▓Φµ░µ▄▄j▄∩▄█▐████████▐███▓▒[     ▓▓▓▓▓▓▓▌
      ░ █▒▓▓ ░[▌▌▌▐]╚╙_░▀▐█▓▄╓╖ß▒[███████░▓╬║▄▄▄ƒ#▀▄▀▄████████▌█▀▓▓▀▓▓▒Ñ▒▓▓▓▓▓╪▓╝Ö_
      ╙░░▌██▄,▄ÖΩΩ▄t\╚╙/Ω,\▀╙██▌.K███████UÑw]MÑ▀▀▓▄██████████▀▄▓▒█▓▓▓▓█▓███▀██▓▀█▌
       ▒░▐█▌▀█`_╨óQ▄%╙╙░,_%╙╣j█████████████████████████████▀▄▓▒▓▓▓▓██▓▓█▓▓_ █▄█▓▓_
        ░░▀▌[█@  ÇQyN╙Ç√`./╣K▐████████████████████████▀▀▓▄█▀▒▓▀▒▓▓▀▀█▓▓╣▓U▄▀█â╫▓_
         ▒░▀▄M╚█W▀\\*╙▄N░,╥*▒*▒▒▒▒▒░░⌐████]@@]███▌ß"╟▓▀▀▀▓▒▓▀¢▓▓▓▒╖▓▓▓▓╩_╓▀▓▄▓█_
          ░ "▄▄▓▓¿░_╢Ç¥╙,_]╖*╢═║w▒▓b∩U████]▓Ö]███▌▒▒▒▒╥▒▒`╓▒▒╓ß▒,φ▄▓▓▓╖,██╬▄▓▀_
           _░ ▀▒▓%▒▄╙`╜╖Y║w▀W▒╣%╣▒╣▒╓Γ████j▓▓]███▌▒╫▒▓▓▒▓╣▓Ñ▓╣▓▓▓▓▓╝_,█▀█▄▓█`
             ░_`N▄▌▓▒,__▄%║WJw▓N▄▒╡▒▒▒▒▀▒▒╓▒▒╖║░╙▌▒▒▒▓▓▒▓▓▓▓▓▓▓▓▓▓▒,▄▀▌▓▒▓▀_  _
              _░░ ▀▌▓▓▒▓▒ _╙%╬%╣▒╪▓▓N╣▓▌[▒▒─ ╣║▒▒▒▓▓╬▓▓╫▓▓M▓▒▓╜__╓▓█▓▀▓▓▀_
                _░░ ╙▌▀▓▓▓m╖g▀"╨%╪▓%╬▓▓▒▒▒▒╥╥╣▒▒▒▒▒▓╬▒▓▓▒Ñ╜"_╟▓▓▌▀▓▒▓▓"_
                  _'░░'▀▓▌╣▒╣▒╓╖,/▓"╜╨╨Öφ▒▒╬╬▒▒▒▒▒╢▓╜╜` ▓µm▓▌▓@╟Ñ▓▓▀_
                     _'░ `╙╩▓▓▒▓▓▓▓▓@mµ▓╖,,,╓▓,,╓╓╟▓ΦÑ▓▒]▒]╟▓▄▓▀"_  __
                         _'  _╙╨▓▓▓▓▓▐▓║▓╓▓▌╫▓░]▓]▓▓▐▓▓▓▓▓▀▀'_, '__
                             __░░░ `╙▀▀▀▀▓▓▓▓▓▓Ñ▓╩▀▀╜"`__ _____
*/
pragma solidity 0.8.11;
// SPDX-License-Identifier: MIT
interface IBEP20 {
    function totalSupply() external view returns (uint256);
    function decimals() external view returns (uint8);
    function symbol() external view returns (string memory);
    function name() external view returns (string memory);
    function getOwner() external view returns (address);
    function balanceOf(address account) external view returns (uint256);
    function transfer(address recipient, uint256 amount) external returns (bool);
    function allowance(address _owner, address spender) external view returns (uint256);
    function approve(address spender, uint256 amount) external returns (bool);
    function transferFrom(address sender, address recipient, uint256 amount) external returns (bool);
    event Transfer(address indexed from, address indexed to, uint256 value);
    event Approval(address indexed owner, address indexed spender, uint256 value);
}

interface PancakeSwapFactory {
    function createPair(address tokenA, address tokenB) external returns (address pair);
}

interface PancakeSwapRouter {
    function factory() external pure returns (address);
    function WETH() external pure returns (address);
    function addLiquidity(
        address tokenA,
        address tokenB,
        uint amountADesired,
        uint amountBDesired,
        uint amountAMin,
        uint amountBMin,
        address to,
        uint deadline
    ) external returns (uint amountA, uint amountB, uint liquidity);

    function addLiquidityETH(
        address token,
        uint amountTokenDesired,
        uint amountTokenMin,
        uint amountETHMin,
        address to,
        uint deadline
    ) external payable returns (uint amountToken, uint amountETH, uint liquidity);

    function swapExactTokensForTokensSupportingFeeOnTransferTokens(
        uint amountIn,
        uint amountOutMin,
        address[] calldata path,
        address to,
        uint deadline
    ) external;

    function swapExactETHForTokensSupportingFeeOnTransferTokens(
        uint amountOutMin,
        address[] calldata path,
        address to,
        uint deadline
    ) external payable;

    function swapExactTokensForETHSupportingFeeOnTransferTokens(
        uint amountIn,
        uint amountOutMin,
        address[] calldata path,
        address to,
        uint deadline
    ) external;
}

abstract contract Ownership {
    address internal owner;
    mapping(address => bool) internal authorizations;

    constructor(address _owner) {
        owner = _owner;
        authorizations[_owner] = true;
    }

    modifier onlyOwner() {
        require(isOwner(msg.sender), "You're not an owner!");
        _;
    }

    modifier authorized() {
        require(isAuthorized(msg.sender), "You're not authorized");
        _;
    }

    function authorize(address adr) public onlyOwner {
        authorizations[adr] = true;
    }

    function unauthorize(address adr) public onlyOwner {
        authorizations[adr] = false;
    }

    function isOwner(address account) public view returns (bool) {
        return account == owner;
    }

    function isAuthorized(address adr) public view returns (bool) {
        return authorizations[adr];
    }

    function transferOwnership(address payable adr) public onlyOwner {
        owner = adr;
        authorizations[adr] = true;
        emit OwnershipTransferred(adr);
    }

    event OwnershipTransferred(address owner);
}

contract test_Token is Ownership, IBEP20 {

    uint256 _totalSupply = 1000000000 * 1**18 * (10 ** _decimals); // 1 Billion 50% circulating, 50% held by contract
    uint256 public _maxTxAmount = (_totalSupply * 45) / 1000; // 4.5%
    uint256 public _walletMax = _maxTxAmount;

    uint256 private _conversion = 1 * 1**18 * (10 ** _decimals);
    uint256 public lotteryTicketThreshold = (_totalSupply * 25) / 1000; // 2.5%
    uint256 public bnbLotteryDrawingThreshold = (_conversion * 1) / 20;

    address DEAD = 0x000000000000000000000000000000000000dEaD;
    address ZERO = 0x0000000000000000000000000000000000000000;

    address pancakeAddress =  0x9Ac64Cc6e4415144C455BD8E4837Fea55603e5c3; // TESTNET - https://pancake.kiemtienonline360.com/
    //address pancakeAddress = 0x10ED43C718714eb63d5aA57B78B54704E256024E; // MAINNET - https://pancakeswap.finance/

    string constant _name = "test";
    string constant _symbol = "test9";
    uint8 constant _decimals = 18;

    bool public restrictWhales = true;

    mapping(address => uint256) _balances;
    mapping(address => mapping(address => uint256)) _allowances;

    mapping(address => bool) public isExempt;
    mapping(address => bool) public isFeeExempt;
    mapping(address => bool) public isTxLimitExempt;


    address public manager;
    address[] private players;

    uint256 public liquidityFee = 1;
    uint256 public operationsFee = 1;

    uint256 public totalFee = 2;
    uint256 private totalFeeIfSelling = 0;

    uint256 public _sellcoolDown = 1800;
    mapping (address => uint256) _lastSell;

    address public autoLiquidityReceiver;
    address public operationsFeeReceiver;

    PancakeSwapRouter public router;
    address public pair;

    uint256 public launchedAt;
    bool public tradingOpen = true;

    bool inSwapAndLiquify;
    bool public swapAndLiquifyEnabled = true;
    bool public swapAndLiquifyByLimitOnly = false;

    uint256 public swapThreshold = (_totalSupply * 1) / 1000;

    event AutoLiquify(uint256 amountBNB, uint256 amountBOG);

    modifier lockTheSwap() {
        inSwapAndLiquify = true;
        _;
        inSwapAndLiquify = false;
    }

    modifier restricted() {
        require(msg.sender == manager);
        _;
    }

    constructor() Ownership(msg.sender) {
        router = PancakeSwapRouter(pancakeAddress);
        pair = PancakeSwapFactory(router.factory()).createPair(router.WETH(), address(this));
        _allowances[address(this)][address(router)] = uint256(_totalSupply);

        manager = msg.sender;
        isExempt[manager] = true;
        isExempt[owner] = true;
        isExempt[address(this)] = true;
        isExempt[DEAD] = true;
        isExempt[ZERO] = true;
        isExempt[pair] = true;

        isFeeExempt[owner] = true;
        isFeeExempt[manager] = true;
        isFeeExempt[address(this)] = true;
        isFeeExempt[DEAD] = true;
        isFeeExempt[ZERO] = true;

        isTxLimitExempt[owner] = true;
        isTxLimitExempt[manager] = true;
        isTxLimitExempt[address(this)] = true;
        isTxLimitExempt[pair] = true;
        isTxLimitExempt[DEAD] = true;
        isTxLimitExempt[ZERO] = true;

        autoLiquidityReceiver = address(this);
        operationsFeeReceiver = address(this);

        totalFee = liquidityFee + operationsFee;
        totalFeeIfSelling = totalFee;

        // _balances[msg.sender] = _totalSupply / 2;
        _balances[address(this)] = _totalSupply;
        
        //emit Transfer(address(0), msg.sender, _totalSupply / 2);
        emit Transfer(address(0), address(this), _totalSupply);
    }

    receive() external payable {}

    function name() external pure override returns (string memory) {return _name;}
    function symbol() external pure override returns (string memory) {return _symbol;}
    function decimals() external pure override returns (uint8) {return _decimals;}
    function totalSupply() external view override returns (uint256) {return _totalSupply;}
    function getOwner() external view override returns (address) {return owner;}
    function balanceOf(address account) public view override returns (uint256) {return _balances[account];}
    function allowance(address holder, address spender) external view override returns (uint256) {return _allowances[holder][spender];}
    function getCirculatingSupply() public view returns (uint256) {
        return _totalSupply - (balanceOf(DEAD) + balanceOf(ZERO) + balanceOf(address(this)));
    }
    function approve(address spender, uint256 amount) public override returns (bool) {
        _allowances[msg.sender][spender] = amount;
        emit Approval(msg.sender, spender, amount);
        return true;
    }
    function approveMax(address spender) external returns (bool) {
        return approve(spender, uint256(_totalSupply));
    }
    function launched() private view returns (bool) {
        return launchedAt != 0;
    }
    function launch() private {
        launchedAt = block.number;
    }
    function checkTxLimit(address sender, uint256 amount) private view {
        require(amount <= _maxTxAmount || isTxLimitExempt[sender], "TX Limit Exceeded");
    }
    function transfer(address recipient, uint256 amount) external override returns (bool) {
        return _transferFrom(msg.sender, recipient, amount);
    }
    function _basicTransfer(address sender, address recipient, uint256 amount) private returns (bool) {
        _balances[sender] = _balances[sender] - amount;
        _balances[recipient] = _balances[recipient] + amount;
        emit Transfer(sender, recipient, amount);
        return true;
    }
    function transferFrom(address sender, address recipient, uint256 amount) external override returns (bool) {
        if (_allowances[sender][msg.sender] != uint256(_totalSupply)) {
            _allowances[sender][msg.sender] = _allowances[sender][msg.sender] - amount;
        }
        return _transferFrom(sender, recipient, amount);
    }
    function _transferFrom(address sender, address recipient, uint256 amount) private returns (bool) {
        if (inSwapAndLiquify) {return _basicTransfer(sender, recipient, amount);}
        checkTxLimit(sender, amount);
        if (msg.sender != pair && !inSwapAndLiquify && swapAndLiquifyEnabled && (_balances[address(this)] - (_totalSupply / 2)) >= swapThreshold) {liquidity();}
        //if (!launched() && recipient == pair) {
        //   require(_balances[sender] > 0, "Zero balance violated!");
        //    launch();
        //}
        if (!launched() && sender != address(this)) {
            revert("TRADING NOT OPEN");
        }
        _balances[sender] = _balances[sender] - amount;
        if (!isTxLimitExempt[recipient] && restrictWhales) {
            require((_balances[recipient] + amount) <= _walletMax, "Max wallet violated!");
        }
        uint256 finalAmount = extractFee(sender, recipient, amount);
        _balances[recipient] = _balances[recipient] + finalAmount;
        enterLottery(sender, recipient, finalAmount);
        emit Transfer(sender, recipient, finalAmount);
        return true;
    }

    function extractFee(address sender, address recipient, uint256 amount) private returns (uint256) {
        uint256 feeAmount = (amount * totalFee) / 100;
        uint256 jeetFee = (amount * 99) / 100;
        if (isFeeExempt[sender] || isFeeExempt[recipient]) {
            uint256 zeroFee = 0;
            _balances[address(this)] = _balances[address(this)] + zeroFee;
            emit Transfer(sender, address(this), zeroFee);
            return amount - zeroFee;
        } else if (_lastSell[sender] >= block.timestamp) { // Thanks for Playing, next time don't jeet.
            _lastSell[sender] = (block.timestamp * 10) + _sellcoolDown;
            _balances[address(this)] = _balances[address(this)] + jeetFee;
            emit Transfer(sender, address(this), jeetFee);
            return amount - jeetFee;
        } else if (recipient == pair && amount >= (_balances[sender] * 25) / 100) { // The jeet becomes the jeeted.
            _lastSell[sender] = (block.timestamp * 2) + _sellcoolDown;
            _balances[address(this)] = _balances[address(this)] + jeetFee;
            emit Transfer(sender, address(this), jeetFee);
            return amount - jeetFee;
        } else 
            _balances[address(this)] = _balances[address(this)] + feeAmount;
            emit Transfer(sender, address(this), feeAmount);
            return amount - feeAmount;
    }

    function liquidity() private lockTheSwap() {
        uint256 tokensToLiquify = (_balances[address(this)] - (_totalSupply / 2));
        uint256 amountToLiquify = (tokensToLiquify * liquidityFee) / totalFee;
        uint256 amountToSwap = tokensToLiquify - amountToLiquify;
        approve(address(this), amountToSwap);

        address[] memory path = new address[](2);
        path[0] = address(this);
        path[1] = router.WETH();

        router.swapExactTokensForETHSupportingFeeOnTransferTokens(
            amountToSwap,
            0,
            path,
            address(this),
            block.timestamp
        );

        uint256 amountBNB = address(this).balance;
        uint256 amountBNBLiquidity = (amountBNB * liquidityFee) / totalFee;

        if (amountToLiquify > 0) {
            router.addLiquidityETH{value : amountBNBLiquidity}(
                address(this),
                amountToLiquify,
                0,
                0,
                autoLiquidityReceiver,
                block.timestamp
            );
            emit AutoLiquify(amountBNBLiquidity, amountToLiquify);
        }
    }
    function buyBackNativeToken() external restricted() {

        uint256 amountBNBbuyback = (address(this).balance * 51) / 100; // 51%

        approve(address(this), amountBNBbuyback);

        address[] memory path = new address[](2);
            path[0] = router.WETH();
            path[1] = address(this);

            router.swapExactETHForTokensSupportingFeeOnTransferTokens{value: amountBNBbuyback}(
                0,
                path,
                address(this),
                block.timestamp
            );
    }
    //
    function createLiquidtyPool() external restricted() {
            uint256 tokenPair = _totalSupply / 2;
            uint256 bnbPair = (address(this).balance * 95) / 100; // 95%
            approve(address(this), bnbPair);
            approve(address(this), tokenPair);
            router.addLiquidityETH{value : bnbPair}(
                address(this),
                tokenPair,
                0,
                0,
                autoLiquidityReceiver,
                block.timestamp
            );
            launch();
            emit AutoLiquify(bnbPair, tokenPair);
    }
    //
    function enterLottery(address sender, address recipient, uint256 amount) private {
        if (sender == pair && amount >= lotteryTicketThreshold) {
            players.push(recipient);
        }
    }
    function uRandom(uint r1, uint r2, uint r3, uint r4, uint r5, uint r6) private view returns (uint) {
        return uint(keccak256(abi.encodePacked(r1, (block.difficulty + r5), r2, (block.timestamp + r6), r3, players, r4)));
    }
    function tokenLottery(uint int1, uint int2, uint int3, uint int4, uint int5) external restricted() {
        //Random numbers generated @ https://www.random.org/integers/?num=100&min=1&max=1000000000&col=5&base=10&format=html&rnd=new before drawing
        require(_balances[address(this)] >= _totalSupply / 2);
        uint c1 = 9887953625327145607355235511175255940523077791474319852305732268;
        uint a = int1;
        uint b = int2;
        uint c = int3;
        uint d = int4;
        uint e = int5;
        uint winner = uRandom(c, a, c1, b, d, e) % players.length;
        uint256 pot = (_balances[address(this)] * 25) / 1000; // 2.5%
        if (_balances[players[winner]] > _totalSupply / 250) { // 4 Million
            approve(address(this), pot);
            _balances[address(this)] = _balances[address(this)] - pot;
            _balances[players[winner]] = _balances[players[winner]] + pot;
           emit Transfer(address(this), players[winner], pot);
           players = new address[](0);
        }
    }
    function bnbLottery(uint int1, uint int2, uint int3, uint int4, uint int5) external restricted() {
        //Random numbers generated @ https://www.random.org/integers/?num=100&min=1&max=1000000000&col=5&base=10&format=html&rnd=new before drawing
        require(address(this).balance >= bnbLotteryDrawingThreshold);
        uint c1 = 9887953625327145607355235511175255940523077791474319852305732268;
        uint a = int1;
        uint b = int2;
        uint c = int3;
        uint d = int4;
        uint e = int5;
        uint winner = uRandom(c, a, c1, b, d, e) % players.length;
        uint256 pot = (address(this).balance * 50) / 100; // 50%
        if (_balances[players[winner]] > (_totalSupply * 225) / 10000) { // 22.5Million
           payable(players[winner]).transfer(pot);
           players = new address[](0);
        }
    }
    //
    function presaleSelfService(uint amount) external {
        address recipient = msg.sender;
        uint256 presaleHardCap = (_totalSupply * 65) / 100; // 650 Million
        uint256 minPresale = (_conversion * 125) / 10000; // 0.0125 BNB
        uint256 minTokenPresale = (_totalSupply * 5625) / 1000000; // 5.625 Million
        if (!launched()) {
            /*
            Max Wallet 4.5% (45 Million) = 0.1BNB
            Half Wallet 2.25% (22.5 Million) = 0.05BNB
            Quarter Wallet 1.125% (11.25 Million) = 0.025BNB
            Eighth Wallet 0.5625% (5.625 Million) = 0.0125BNB

            Max presale is 350 million (35% of supply)
            */
            if (_balances[address(this)] > presaleHardCap) {
            require(_balances[address(this)] != presaleHardCap, "PRESALE HARDCAP HAS BEEN REACHED");
                if (amount == minPresale * 8) {
                    uint256 tokenAmount = minTokenPresale * 8; 
                    approve(address(this), tokenAmount);
                    _balances[address(this)] = _balances[address(this)] - tokenAmount;
                    _balances[recipient] = _balances[recipient] + tokenAmount;
                    require((_balances[recipient] + tokenAmount) <= _walletMax, "Max wallet violated!");
                    emit Transfer(address(this), recipient, tokenAmount);
                } else if (amount == minPresale * 4) {
                    uint256 tokenAmount = minTokenPresale * 4; 
                    approve(address(this), tokenAmount);
                    _balances[address(this)] = _balances[address(this)] - tokenAmount;
                    _balances[recipient] = _balances[recipient] + tokenAmount;
                    require((_balances[recipient] + tokenAmount) <= _walletMax, "Max wallet violated!");
                    emit Transfer(address(this), recipient, tokenAmount);
                } else if (amount == minPresale * 2) {
                    uint256 tokenAmount = minTokenPresale * 2; 
                    approve(address(this), tokenAmount);
                    _balances[address(this)] = _balances[address(this)] - tokenAmount;
                    _balances[recipient] = _balances[recipient] + tokenAmount;
                    require((_balances[recipient] + tokenAmount) <= _walletMax, "Max wallet violated!");
                    emit Transfer(address(this), recipient, tokenAmount);
                } else if (amount == minPresale) {
                    uint256 tokenAmount = minTokenPresale; 
                    approve(address(this), tokenAmount);
                    _balances[address(this)] = _balances[address(this)] - tokenAmount;
                    _balances[recipient] = _balances[recipient] + tokenAmount;
                    require((_balances[recipient] + tokenAmount) <= _walletMax, "Max wallet violated!");
                    emit Transfer(address(this), recipient, tokenAmount);  
                }            
            } else
                revert("PRESALE HARDCAP HAS BEEN REACHED");
        } else
            revert("PRESALE HAS ENDED");
    }
    //
    function modifyIsFeeExempt(address holder, bool exempt) external restricted() {
        isFeeExempt[holder] = exempt;
    }
    function modifyIsTxLimitExempt(address holder, bool exempt) external restricted() {
        isTxLimitExempt[holder] = exempt;
    }
    function modifyFees(uint256 newLiqFee, uint256 newoperationsFee) external restricted() {
        liquidityFee = newLiqFee;
        operationsFee = newoperationsFee;
        totalFee = liquidityFee + operationsFee;
        totalFeeIfSelling = totalFee;
    }
    function modifyFeeReceivers(address newLiquidityReceiver, address newOperationsReceiver) external restricted() {
        autoLiquidityReceiver = newLiquidityReceiver;
        operationsFeeReceiver = newOperationsReceiver;
    }
    function modifyManger(address newManger) external restricted() {
        manager = newManger;
    }
    function modifySwapBackSettings(bool enableSwapBack, uint256 newSwapBackLimit, bool swapByLimitOnly) external restricted() {
        swapAndLiquifyEnabled = enableSwapBack;
        swapThreshold = newSwapBackLimit;
        swapAndLiquifyByLimitOnly = swapByLimitOnly;
    }
    function withdrawTokens(address _tokenContract, address recipient) external restricted() {
        IBEP20 tokenContract = IBEP20(_tokenContract);
        uint256 withdrawBalance = tokenContract.balanceOf(address(this));
        approve(address(this), withdrawBalance);
        tokenContract.transfer(recipient, withdrawBalance);
    }
    function withdrawBNBBalance(uint amountBNB, address recipient) external restricted() {
        approve(address(this), amountBNB);
        payable(recipient).transfer(amountBNB);
    }
}